auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false

# vite-plugin-pwa's virtual module imports this and resolves from the workspace
# root, where pnpm's strict layout would not otherwise place it.
public-hoist-pattern[]=workbox-window
